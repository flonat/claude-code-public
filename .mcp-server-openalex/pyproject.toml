[project]
name = "openalex-mcp"
version = "0.1.0"
description = "MCP server exposing OpenAlex scholarly search as native tools"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "requests",
]
